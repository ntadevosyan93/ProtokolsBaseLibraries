CREATE TABLE [dbo].[KAD$WORK_TIME_Curator_day] (
    [guid]    VARCHAR (50) NOT NULL,
    [curator] VARCHAR (50) NOT NULL,
    [day]     VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_KAD$WORK_TIME_Curator_day] PRIMARY KEY CLUSTERED ([guid] ASC)
);

