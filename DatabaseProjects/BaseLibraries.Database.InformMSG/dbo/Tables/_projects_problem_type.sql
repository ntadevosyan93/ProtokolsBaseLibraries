CREATE TABLE [dbo].[_projects_problem_type] (
    [id]   INT           IDENTITY (1, 1) NOT NULL,
    [text] VARCHAR (150) NULL,
    CONSTRAINT [PK__projects_problem_type] PRIMARY KEY CLUSTERED ([id] ASC)
);

