CREATE TABLE [dbo].[_projects_problems_status] (
    [id]   INT           IDENTITY (1, 1) NOT NULL,
    [text] VARCHAR (150) NULL,
    CONSTRAINT [PK__projects_problems_status] PRIMARY KEY CLUSTERED ([id] ASC)
);

